package linzx.homework.trains.algorithm;

public interface Dijkstra {
    /**
     * For example: "The length of the shortest routes (in terms of distance to travel) 
     * from station A to other stations"
     **/
    int[] calcShortestRoutes(char station);
}
